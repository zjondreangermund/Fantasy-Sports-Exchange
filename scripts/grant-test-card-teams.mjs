#!/usr/bin/env node

console.error([
  "The full-set test card grant has been permanently retired.",
  "It bypassed normal mint provenance and supply controls and must not be used against Fantasy Arena data.",
  "Use signup, tournament rewards, weekly rewards, referrals, replacement claims, marketplace purchases, or approved admin mint flows instead.",
].join("\n"));
process.exit(1);
